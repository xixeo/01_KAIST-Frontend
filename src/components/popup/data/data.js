export default function getData() {
  return {
    draggable: {
      use: true,
      iconClass: 'pushpin_off'
    },
    fullscreen: {
      use: true,
      iconClass: 'fullscreen',
      backColor: 'currentColor'
      // fill: { use: true, percent: 50, color: '#ffff00', x1: '100%', y1: '0%', x2: '0%', y2: '0%' }
    },
    close: {
      use: true,
      iconClass: 'close'
    }
  }
}
